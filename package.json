{
  "name": "now-playing",
  "version": "2.3.0",
  "description": "Console-style Spotify, YouTube Music, local and Windows media playback with fullscreen visuals for Decky Loader.",
  "author": "LoZazaMastro",
  "type": "module",
  "scripts": {
    "build": "node scripts/validate-translations.mjs && tsc --noEmit --noUnusedLocals --noUnusedParameters && rollup -c",
    "watch": "rollup -c -w",
    "package:win": "powershell -ExecutionPolicy Bypass -File ./package-win.ps1",
    "check:i18n": "node scripts/validate-translations.mjs",
    "check:types": "tsc --noEmit --noUnusedLocals --noUnusedParameters",
    "check:python": "python -m py_compile main.py ytmusic_service.py && python scripts/validate-runtime.py",
    "verify": "node scripts/validate-translations.mjs && tsc --noEmit --noUnusedLocals --noUnusedParameters && python -m py_compile main.py ytmusic_service.py && python scripts/validate-runtime.py && node --check dist/index.js"
  },
  "license": "MIT",
  "dependencies": {
    "@decky/api": "^1.1.3",
    "@decky/ui": "^4.11.0",
    "react-icons": "^5.3.0",
    "three": "0.158.0",
    "tslib": "^2.7.0"
  },
  "devDependencies": {
    "@decky/rollup": "^1.0.2",
    "@types/react": "19.1.1",
    "@types/react-dom": "19.1.1",
    "@types/three": "0.158.3",
    "rollup": "^4.53.3",
    "typescript": "^5.6.2"
  }
}
